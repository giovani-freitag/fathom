/**
 * What the panels are called in the document.
 *
 * Here rather than beside the panels themselves: a control that reveals one
 * has to name it, and the editor is fetched only when it is opened — importing
 * the id from it would pull a compiler into the bar that opens it.
 */

/** The editor a reader writes a reading in. */
export const ADDON_EDITOR_ID = 'addon-editor';
